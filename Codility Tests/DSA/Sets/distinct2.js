/*
-Solving the level 6 problem using sets
*/

function solution(A){
    const uniqueValues = new Set(A);
    //initialize our set
    return uniqueValues.size;
    //return the number of unique values
}